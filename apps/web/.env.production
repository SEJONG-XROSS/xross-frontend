# 프로덕션 빌드(`vite build`) 시 자동 로드됨.
# 브라우저는 same-origin(/api, /mediamtx) HTTPS 로 요청 → Vercel Edge 가
# vercel.json rewrites 를 통해 HTTP 백엔드로 server-side proxy.
# Mixed Content 차단을 우회하는 목적. 백엔드 HTTPS 화가 되면 절대 URL로 복귀.
VITE_API_BASE_URL=/api
VITE_MEDIAMTX_BASE_URL=/mediamtx
